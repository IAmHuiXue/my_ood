package uml;

public class Relationship {
    /**
     * Animal depends on water --> Dependency
     * Bird, mammal "is-a" animal --> Generalization
     * Bird is composed by wing --> Composition (same lifecycle)
     * DonalDuck is capable of talk --> Interface Realization
     * Wolf aggregates wolfpack --> Aggregation (not same lifecycle)
     * Wolf associates with sheep --> Association (could be single directional or dual)
     *
     * Relationship Strength (Strong to Weak)
     * Generalization = Realization > Composition > Aggregation > Association > Dependency
     *
     */
}
